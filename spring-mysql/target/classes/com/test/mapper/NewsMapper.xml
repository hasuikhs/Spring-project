<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.NewsMapper">

	<select id="getList" resultType="Map">
		<!-- CDATA는 안에 들어가는 테스트가 파싱되지 않게 하는 기능 -->
		/* NewsMapper.getList */
		<![CDATA[
			SELECT date, title, content, (SELECT name FROM reporter WHERE reporter.userno=news.userno) FROM news
		]]>
	</select>

	<select id="read" resultType="Map">
		SELECT date, title, content,
		(SELECT name FROM reporter WHERE reporter.userno=news.userno)
		FROM news
		WHERE newsno = #{newsno}
	</select>

	<insert id="insert">
		/* NewsMapper.insert */
		INSERT INTO news (userno,
		date, title, content)
		VALUES (#{userno}, #{date}, #{title}, #{content})
	</insert>

</mapper>